<tr>
    <td><%= attendee.username %></td>
    <% if current_user.authorized?(@event.user_id) %>
      <td><%= link_to 'Remove',attendee, :method => :delete, :confirm => 'Are you sure you want to remove this attendee?' %></td>
    <% end %>
</tr>
